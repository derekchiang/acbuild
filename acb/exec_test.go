package acb

import (
	//"io/ioutil"
	//"path/filepath"
	"testing"

	//"github.com/appc/acbuild/Godeps/_workspace/src/github.com/stretchr/testify/assert"

	//"github.com/appc/acbuild/internal/fixtures"
	//"github.com/appc/acbuild/internal/util"
)

func TestExec(t *testing.T) {
	//s, err := util.GetTmpStore()
	//assert.NoError(t, err, "error getting temp store")

	//tmpDir, err := ioutil.TempDir("", "")
	//assert.NoError(t, err, "error creating tmp dir")

	//outputACI := filepath.Join(tmpDir, "go2.aci")
	//outputImageName := "go2"
	//assert.NoError(t, Exec(s, fixtures.GoACI, outputACI, "/go version", outputImageName, false, true, nil), "error with Exec")
}
